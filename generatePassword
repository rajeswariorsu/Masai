function generatePassword(length, includeNumbers = true, includeSpecialChars = false){
  let upperCase = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
  let lowerCase = "abcdefghijklmnopqrstuvwxyz";
  numbers = "1234567890"
  specialChars = "@#!"
  if(includeNumbers){
    allChar += numbers;
    password += numbers[Math.random()*numbers.length];
  }
  if(includeSpecialChars){
    allChar += specialChars;
    password += specialChars[Math.random()*specialChars.length];
    
  }
  password+= upperCase[Math.random()*upperCase.length]
  
  while(password.length < length){
    password = allChars[[Math.random()*allChars.length]
  }
  return password

}
console.log(generatePassword(10, true, true) )
